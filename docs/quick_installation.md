# Quick installation

Download REST API:

```bash
git clone https://github.com/oktat/stuapi.git
```

Install dependencies:

```bash
cd stuapi
npm install
```

Generate API KEY and migrate:

```bash
node op key:generate
node op migrate
```

Start REST API:

```bash
npm start
```
